# SBTS 2.2.2 — RC7 Dashboard Phase Column Hotfix

## المشكلة

كان استعلام لوحة التحكم يفشل بخطأ داخلي بعد إضافة بيانات حقيقية، رغم أن صفحات
المناطق والمشاريع والـ Blinds تعمل بصورة صحيحة. الاستعلام المولّد لجدول
`workflow_transition_events` كان يطلب عمودًا باسم `phaseKey`، بينما مخطط MySQL
المعتمد يحتوي العمودين `fromPhaseKey` و`toPhaseKey`.

## الإصلاح

- ربط حقلي سجل الانتقال باسميهما الفعليين في MySQL.
- إبقاء قائمة مفاتيح المراحل في مصدر واحد لمنع اختلاف قيم الـ enum.
- إضافة العمودين إلى فحص مخطط قاعدة البيانات الذي يعمل قبل تشغيل Railway.
- إضافة اختبار يفحص SQL المولّد فعليًا ويمنع الرجوع إلى `phaseKey`.

## أثر النشر

- لا توجد Migration جديدة؛ مخطط قاعدة البيانات الحالي صحيح بالفعل.
- لا تحذف MySQL أو منطقة التخزين أو أي بيانات مدخلة.
- لا تغيّر متغيرات Railway الحالية.
- ارفع محتويات RC7 إلى مستودع GitHub نفسه ثم انتظر نشر Railway تلقائيًا.

## التحقق بعد النشر

1. افتح `/dashboard` وتأكد من ظهور أرقام المنطقة والمشروع والـ Blind.
2. افتح الـ Blind الحالي وتأكد أن البيانات السابقة ما زالت موجودة.
3. نفّذ انتقال المرحلة من Workflow Control ثم راجع Activity في الداشبورد.
4. شغّل:

   ```powershell
   pnpm deploy:verify -- https://YOUR-SERVICE.up.railway.app
   ```

يجب أن يعرض `/health` و`/ready` الإصدار `2.2.2` وحالة قاعدة البيانات
`connected`.
