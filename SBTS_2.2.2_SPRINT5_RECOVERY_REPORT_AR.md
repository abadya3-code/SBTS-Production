# تقرير Sprint 5 Recovery — SBTS 2.2.2

تاريخ الإعداد: 2026-07-31

## القرار

هذه الحزمة **Release Candidate** جاهزة للفحص والنشر المنضبط، وليست دليلاً على
أن Railway نُشر فعلاً. قبول Sprint 5 النهائي يتطلب تطابق `/health` و`/ready`
مع الإصدار 2.2.2 وcommit فرع `main` واتصال قاعدة البيانات.

## السبب الجذري لبقاء النسخة القديمة

- الحزمة المستلمة مصدر بلا مجلد `.git`، لذلك تعديلها محلياً لا ينشئ commit ولا
  يرسل شيئاً إلى GitHub.
- صورة `/health` المنشورة أظهرت 2.1.0 بلا commit، بينما المصدر الحالي 2.2.1؛
  وهذا يثبت أن Railway كان يشغّل مصدراً/commit أقدم، وليس مجرد Cache في الواجهة.
- متغير `APP_VERSION` القديم كان يسمح بتضارب هوية الإصدار. أصبح الإصدار الآن
  ثابتاً داخل البناء، وأي قيمة قديمة تُرفض قبل النشر.

## الإصلاحات المنفذة

### النشر وهوية الإصدار

- إصلاح فشل المجلد النظيف: سكربت `02_PUSH_UPDATE` يثبت `node_modules` من
  `pnpm-lock.yaml` تلقائياً قبل الفحص، وDocker يحتفظ بملف CI الذي تفحصه عقود
  الإصدار بدلاً من استبعاده عبر `.dockerignore`.
- توحيد `package.json` و`VERSION` والخادم والواجهة على 2.2.2.
- إظهار commit في `/health` و`/ready` والواجهة.
- جعل Railway وDocker يعتمدان `/ready` الذي يفحص MySQL.
- إضافة فحص Git قبل/بعد push وفحص مستقل للنسخة المنشورة.
- جعل baseline للقراءة فقط وتعطيل `db:push` حتى توحيد تاريخ migrations.
- جعل runtime backfill اختيارياً لأول ترقية بدل تشغيله في كل نشر.

### الأمن والصلاحيات

- منع تسريب حقول مستخدم المصادقة الداخلية باستخدام DTO صريح.
- إبطال JWT الصادر قبل آخر تغيير لكلمة المرور.
- توحيد مسار تغيير كلمة المرور وتطبيق سياستها وتسجيل الحدث.
- تطبيق RBAC من قاعدة البيانات على الوحدات الحساسة.
- حماية `/storage/*` بجلسة مستخدم نشطة واختيار backend صريح.
- فرض استقلال منفذ/متحقق LOTO، ومنفذ/مُعتمد torque، ومنشئ/مُعتمد leak test.
- لم تظهر مؤشرات أكواد خبيثة في مراجعة المصدر؛ أزيلت وحدات Manus/OAuth/AI
  غير المستخدمة وتبعيات عالية الخطورة غير لازمة.

### البيانات والواجهة

- استبدال Dashboard الوهمي بملخص MySQL للـworkflow القياسي ذي 8 مراحل.
- احتساب الإغلاق من lifecycle `CLOSED` فقط وترتيب الأحداث من الأحدث.
- ربط Area → Project → Blind في مسارات التفاصيل والـbreadcrumbs.
- تحديث القالب القياسي وبيانات وصف الصلاحيات في كل system seed دون استبدال
  تخصيصات الأدوار.
- استبدال SheetJS غير الآمن بمولّد OOXML صغير يعتمد `fflate`.

## بوابات التحقق المحلية

نتيجة آخر تشغيل كامل بتاريخ 2026-07-31:

- Release contract: ناجح، 33 ملفاً إلزامياً وهوية 2.2.2 متطابقة.
- TypeScript: ناجح، صفر أخطاء.
- Vitest: ناجح، 29 ملف اختبار و264/264 اختباراً.
- Sprint static contracts: ناجح، 195/195 فحصاً (Sprint 2–5).
- SQL validation: ناجح، 57 عبارة migration مع توازن وبنية سليمة.
- Production build: ناجح، 3031 module للواجهة وبناء خادم Express ناجح.
- Production dependency audit: **0 ثغرات معروفة** (0 Critical/High/Moderate/Low).
- Frontend mock-data imports: صفر؛ Access Control وDashboard مرتبطان بقاعدة البيانات.

## قيود معروفة لا يجوز إخفاؤها

- سجل Drizzle ينتهي عند 0012 بينما migrations المجال 0013–0019 لها runner
  منفصل؛ لذلك جُمّد `db:push`، وتوحيد السجل ضمن Sprint 7.
- تقارير Reports القديمة ما زالت تحتوي بعض إسقاطات الخمس مراحل؛ Dashboard فقط
  أصبح canonical. توحيد التقارير ضمن Sprint 6.
- حزمة JavaScript الرئيسية 1.69 MB قبل gzip؛ البناء ناجح، لكن تقسيم الصفحات
  الثقيلة إلى dynamic chunks هدف أداء صريح في Sprint 6.
- Entry Readiness يحتاج عمود هوية ثابت لمن أعد السجل حتى يصبح فصل المُعد عن
  المُعتمد قابلاً للإثبات تاريخياً؛ يعالج مع migration منضبط ضمن Sprint لاحق.
- حماية التخزين أغلقت الوصول العام، لكن التفويض على مستوى المشروع لكل كائن
  يحتاج نموذج ملكية مشروع أدق ضمن سبرنت الصلاحيات.

## خطوات القبول على Railway

1. انسخ المصدر إلى Git clone الحقيقي مع الاحتفاظ بـ`.git`.
2. احذف `APP_VERSION` واضبط S3 صراحة.
3. شغّل `02_PUSH_UPDATE.cmd`.
4. انتظر Railway Success.
5. شغّل `pnpm deploy:verify -- https://YOUR-SERVICE.up.railway.app`.
6. نفّذ UAT لمسار Area → Project → Blind، الدخول، الصلاحيات، Dashboard، ورفع
   دليل تجريبي.

لا يبدأ Sprint 6 قبل حفظ مخرجات خطوة 5 ونتيجة UAT كدليل قبول.
